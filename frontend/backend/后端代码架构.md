backend/
├── app/                           # 核心应用代码目录
│   ├── __init__.py                # 包初始化文件
│   ├── config.py                  # 配置管理 (读取环境变量、Dify API Keys等)
│   ├── database.py                # 数据库连接初始化与 Session 依赖 (get_db)
│   ├── models/                    # SQLite 数据模型 (定稿的数据库表)
│   │   ├──__init__.py
│   │   ├──auth_model.py
│   │   ├── ……
│   ├── schemas/                   # Pydantic 响应/请求验证模型 (DTO, 接口传输规范)
│   │   ├──__init__.py
│   │   ├──auth_schemas.py
│   │   ├── ……
│   ├── crud/                      # 数据库增删改查逻辑 (数据库原子操作)
│   │   ├──__init__.py
│   │   ├──auth_curd.py
│   │   ├── ……
│   ├── routers/                   # 路由层 (API 接口控制器，供前端调用)
│   │   ├── __init__.py
│   │   ├── auth.py                # 用户认证相关 API (登录、注册)
│   │   └── projects.py            # 项目管理、人工审批与 AI 状态查询核心 API
│   ├── services/                  # 业务逻辑层 (Service Layer，系统核心大脑)
│   │   ├── __init__.py
│   │   ├── dify_client.py         # Dify API 客户端封装 (调用 3 个独立的工作流)
│   │   └── orchestrator.py        # 智能体协同调度中心 (状态机管理、串行调用控制)
│   └── utils/                     # 工具函数层
│       ├── __init__.py
│       └── file_helper.py         # 源码本地写入、目录创建、项目一键打包 ZIP
├── data/                          # 存放本地 SQLite 数据库文件 (在宿主机挂载持久化)
│   ├──project.db                  # 数据库文件
│   ├──images/                     # 用于存放用户头像图片
├── exports/                       # 存放打包生成的项目 ZIP 压缩包 (提供给前端下载)
├── requirements.txt               # 后端依赖包声明文件 (如 fastapi, sqlalchemy 等)
├── Dockerfile                     # 后端 Docker 容器构建文件
└── main.py                        # FastAPI 全局入口启动文件